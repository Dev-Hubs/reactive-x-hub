import { of, fromEvent } from 'rxjs'; 
import { map } from 'rxjs/operators';


import { ReactiveXHub } from './bridge';
let hub = ReactiveXHub.getInstance();

const source$ = hub.observe(0);

source$.pipe(map(item=>item+1)).subscribe(hub.connect());


