import { map , take, combineAll } from 'rxjs/operators';

import { facade } from './facade';
import { observer } from './helpers';

const source$ = facade.observe(0);
const source2$ = facade.observe(1);

source$.pipe(map(item =>  source2$.pipe(take(3))),take(2)).pipe(combineAll()).subscribe(observer);
